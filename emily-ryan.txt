1. What is Semantic HTML? It describes meaning to both the developer and the browser.

    2. What is HTML used for? HTML formats the text, images, etc. that are on a web page.

    3. What is an attribute and where do we put it? Attributes provide additional information about elements. We put an attribute in the start tag (name="value")

    4. What is the h1 tag used for? How many times should I use it on a page? The main heading(title of website), it should only be used once on a page

    5. Name two tags that have required attributes: img (src or alt), a (ref= )

    6. What do we put in the head of our HTML document? 
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>My test page</title>
  </head>

</html> <!-- This goes at the bottom of the document to close out the HTML -->

    7. What is an id? It used to identify a specific HTML element that is used to point to a style in a style sheet (for CSS and JavaScript)

    8. What elements can I add an id to? <h1>through<h6>, <img>, <button>, <p> * is used fas the universal selector

    9. How many times can I use the same id on a page? 1

    10. What is a class? .class selects elements with a specific class attribute. It is used for linking to CSS style sheet. id selector can be used to identify one element, whereas a class can be used to identify more than one.

    11. What elements can I add a class to? <div> <span> <h1> through <h6>

    12. How many times can I use the same class on a page? More than once

    13. How do I get my link to open in a new tab? 
Here's an example
<a href="https://heygrillhey.com/" target="_blank">Hey Grill Hey</a>

    14. What is the alt attribute in the image tag used for? 
It gives an alternate text for an image if that image cannot be displayed. If someone can not view the image it gives information about the image(for screen readers, etc.) It's also helpful for SEO.

    15. How do I reference an id? In HTML: <h1 id='myHeader'>Hello World!</h1>
In CSS: 
myHeader{
	color: red;
}

    16. What is the difference between a section and a div - <section> creates independent sections within a webpage having logically connected content. <div> is an empty container that can be used mainly for stylistic purposes to divide content. 


    17. What is CSS used for? Making the website look pretty

    18. How to we select an element? Example - every h2 on the page 
h2{
	color: green;
}

    19. What is the difference between a class and an id? - Give me an example of when I might use each one :     .class selects elements with a specific class attribute. It is used for linking to CSS style sheet. id selector can be used to identify one element, whereas a class can be used to identify more than one.


    20. How do we select classes in CSS?
.class{
	color:green;
}

    21. How do we select a p element with a single class of “human””?
.human{
	font-size:20px;
}

    22. What is a parent child selector? When would this be useful? 



    23. How do you select all links within a div with the class of sidebar?


    24. What is a pseudo selector? Selects elements that are in a specific state (e.g. there are the first element of their type, or they are being hovered over by the mouse pointer)


    25. What do we use the change the spacing between lines? Line-height


    26. What do we use to change the spacing between letters? Letter-spacing


    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? 
text-transform: uppercase
text-transform: lowercase
text-transform: capitalize


    28. How do I add a 1px border around my div that is dotted and black?
div{
	border-style: dotted;
	border-color: black;
	border-size: 1px;
}



    29. How do I select everything on the page? 
* {

}


    30. How do I write a comment in CSS? 
/* comment */

    31. How do I find out what file I am in, when I am using the command line? 
% pwd

    32. Using the command line - how do I see a list of files/folders in my current folder?
% ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 

% rm -r     (You need to be super careful because it doesn't give you a confirmation to opt in or out for deleting. It's not in the recycle bin. It is just gone)


    34. Why should I use version control? When working with a team, it helps you back up your work. You can work with a team well, store versions properly, and restore past versions as needed. It helps you have a comprehensive grasp of what happened with a project. 


    35. How often should I commit to github? Whenever you finish a step if you are using git. If you're using something else, you could wait a little longer. 

    36. What is the command we would use to push our repo up to github? 
git push -u origin emily-ryan

    37. Walk me through Lambda's git flow. 

1. Fork
2. Add collaborators as necessary
3. Clone
4. Branch
5. Push
6. Open a pull request for you TL


Stretch Questions

    1. What is the difference between an inline element and a block element?
Inline elements do not force a new line to begin in the document flow. Block elements typically cause a line break to occur

    2. What happens when an element is positioned absolutely? 
It doesn't exist in the normal document layout flow. It sits on its own layer separate from everything else. 

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 


    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default

    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 